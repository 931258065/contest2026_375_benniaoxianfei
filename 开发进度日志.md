# OpenVela 手语翻译项目 · 开发进度日志

> 这份日志用大白话记录每天的进度，方便随时回顾。
> 上次更新：2026-08-15

---

## 一、项目一句话

用 ESP32-P4-Function-EV-Board 开发板 + OpenVela 系统，做一台「拍手语手势 → 识别成文字 → 屏幕显示 + 语音播报」的翻译终端。

## 二、当前进度总览（2026-08-17）

```
环境准备      ██████████ 100%  ✅ 完成
源码下载      ██████████ 100%  ✅ 完成（34GB）
模拟器验证    ██████████ 100%  ✅ 跑出 LVGL 界面
移植-代码落地  ██████████ 100%  ✅ 架构层+板级层+构建注册+兼容修复全完成
移植-编译      ██████████ 100%  ✅ 🎉 固件 nuttx.bin 编译成功（591KB）！
真机烧录点亮  ░░░░░░░░░░   0%  ⏳ 正确板子已邮寄，预计 8-18(周一) 到货 → 烧录验证
硬件资料确认  █████████░  90%  🟡 PDF 已解析，传感器型号待板子实确认
模型训练      ███████░░░  70%  🟡 双人数据：浮点跨人验证 95.5%；
                                INT8 量化损失待 QAT 优化
应用开发      ███░░░░░░░  30%  🟡 骨架+UI预览+网页服务+语音全备，待板子接真
文档答辩      ███████░░░  70%  🟡 README/AI日志/答辩提纲/拍摄指南已完成
```

**里程碑**：**M1（编译层面）达成**——`nuttx.bin` 已生成（Windows 备份在 `vm_exchange/nuttx.bin`）。作品已合入官方仓库 `dev-ai-contest-2026` 分支（PR #2 merged）。**正确开发板已邮寄，预计下周一（8-18）到货**，届时进入真机烧录验证。

## 三、今天完成了什么（2026-08-11，完整记录）

### 第一阶段：环境搭建（上午~中午）

| 事项 | 说明 | 状态 |
|------|------|------|
| 搭好 Ubuntu 22.04 虚拟机 | 在 Windows 里虚拟一台 Ubuntu 电脑（OpenVela 只能在 Ubuntu 编译），用命令行全自动安装 | ✅ |
| 修好终端打不开的问题 | 找到病根：系统语言编码不是 UTF-8，终端拒绝启动；已修复 | ✅ |
| 装好开发工具 | git / cmake / python / 编译器 / Git LFS / repo | ✅ |
| 下载 OpenVela 源码 | 34GB，用 repo 工具从 Gitee 拉取（清华源 repo 不可用，改用 GitHub 源解决） | ✅ |
| 编译模拟器并跑起来 | goldfish 模拟器编译成功（5 分钟），启动后看到 **LVGL 图形界面** | ✅ |
| 验证 LVGL 可用 | 确认 OpenVela 图形框架正常（项目"屏幕显示"功能的前置验证） | ✅ |

### 第二阶段：移植调研（下午）

| 事项 | 说明 | 状态 |
|------|------|------|
| 摸清 OpenVela 板卡支持现状 | 确认 ESP32-P4 **官方未支持**，需自行移植 | ✅ |
| 找到移植来源 | 上游 Apache NuttX 有现成 esp32p4 代码可复用 | ✅ |
| 验证 HAL 版本 | 确认 OpenVela 固定的硬件库版本太旧（无 P4），需升级 | ✅ |
| 出调研报告 | `移植调研报告_ESP32P4.md`：文件清单 + 风险 + 步骤 | ✅ |

### 第三阶段：移植代码落地（下午~傍晚，重头戏）

| 步骤 | 说明 | 状态 |
|------|------|------|
| ① 芯片代码 9 个文件 | 从上游拉取，复制进内核 `arch/risc-v/src/esp32p4/` | ✅ |
| ② 芯片头文件 | `arch/risc-v/include/esp32p4/` | ✅ |
| ③ 内核注册芯片 | `arch/risc-v/Kconfig` 加 ARCH_CHIP_ESP32P4（3 处） | ✅ |
| ④ 硬件库版本升级 | `9fc713a9` → `8d0a8989`（含 esp32p4） | ✅ |
| ⑤ 板级代码 53 个文件 | 复制进 `boards/risc-v/esp32p4/`（含 common + 板级） | ✅ |
| ⑥ 内核注册板级 | `boards/Kconfig` 加 ESP32P4 开发板（3 处） | ✅ |
| ⑦ 芯片系列配置 | `common/espressif/Kconfig` 加 ESPRESSIF_ESP32P4 | ✅ |
| ⑧ 构建系统补齐 | 补 `common/espressif/CMakeLists.txt` + 修变量名 | ✅ |

### 第四阶段：首次编译排错（傍晚~晚上）

**编译排掉了 5 个报错，每次都是「看报错 → 找原因 → 修复 → 再试」：**

| # | 报错 | 原因 | 解决 |
|---|------|------|------|
| 1 | No CMakeLists.txt at vendor 板级 | vendor 板级构建需要 CMakeLists | 改用 nuttx 板级路径构建 |
| 2 | common/espressif 无 CMakeLists | OpenVela 缺 CMake 支持 | 补上游 CMakeLists.txt |
| 3 | `_flat_memory.ld` 空前缀 | 芯片系列没设置，默认选了 C3 | defconfig 加 `ESPRESSIF_CHIP_SERIES="esp32p4"` |
| 4 | 链接脚本用 esp32c3 名字 | 同上 | 同上 |
| 5 | could not load cache / HAL 被删 | 构建目录残留缓存、脚本误删 HAL | 保存 HAL + 清理缓存 + 重建 |
| 6 | HAL 下载卡住（网络） | 虚拟机访问 GitHub 不稳 | 改浅克隆 + 重试，**主库成功** |

### 当前状态

```
主硬件库 esp-hal-3rdparty  ✅ 已下载到本地（~/hal_save）
子模块 mbedtls（加密库）    🟡 等 GitHub 网络恢复（后台重试中）
首次完整编译               ⏳ 还差 mbedtls 下载完成 → 即可编译
```

## 四、移植进度详解（今天重头戏）

### 什么是「移植」？
把 OpenVela 系统从「它本来支持的板子」搬到「你的 ESP32-P4 板子」上，
让它认识你板子的每个零件（CPU、内存、串口、摄像头、屏幕…）。

### 已经完成的部分（代码已搬进 OpenVela 内核）：

```
① 芯片代码（9 个文件）→ 内核 arch/risc-v/src/esp32p4/
   ✅ Kconfig（芯片配置）、Make.defs（编译规则）、
      hal_esp32p4.mk（硬件库文件清单）、esp_chip_rev.c 等

② 芯片头文件 → 内核 arch/risc-v/include/esp32p4/

③ 内核注册：让系统认识这颗芯片
   ✅ arch/risc-v/Kconfig 加了 ARCH_CHIP_ESP32P4
   ✅ boards/Kconfig 加了 ESP32P4 开发板
   ✅ 硬件库版本升级（老版本没有 P4，升级到含 P4 的版本）

④ 板级代码（53 个文件）→ 内核 boards/risc-v/esp32p4/
   ✅ 启动代码 boot、外设初始化 bringup、
      链接脚本 ld、GPIO/按键/网口等驱动

⑤ 构建系统打通
   ✅ 补了缺失的 CMakeLists.txt（硬件库专用）
   ✅ 修复构建脚本变量名（上游和 OpenVela 命名不同）
```

### 卡住的地方：下载硬件库（HAL）→ 已部分解决，剩一个子模块

- **进展**：主硬件库 esp-hal-3rdparty 已用「浅克隆 + 重试」**成功下载**（21MB，已 checkout 到需要版本 8d0a8989）✅
- **剩余卡点**：HAL 里还带一个子模块 **mbedtls**（加密库），它也要从 GitHub 下载，目前网络被拒绝
- **已处理**：后台重试脚本在跑（30 次机会），GitHub 网络是间歇性的，恢复后会自动拉成功
- **本质**：**纯网络问题，不是代码问题**——网络好时这条就通了

### 移植代码与构建的完成度（远超预期）

原以为「建板级目录 + 写配置 + 编译」要很久，实际今天已完成：
1. 板级代码 53 个文件 ✅
2. boards/Kconfig 注册 esp32p4 板 ✅
3. 芯片系列配置（C3/C6/H2 加 P4）✅
4. 补 common/espressif 的 CMakeLists ✅
5. 修复构建变量名（NUTTX_BINARY_DIR→CMAKE_BINARY_DIR）✅
6. defconfig 补芯片系列参数 ✅
7. 首次编译走通到「HAL 下载 + mbedtls 子模块」✅（已排除 5 个构建报错）

### 为什么这批工作很关键？
- 从「OpenVela 根本不认识 esp32p4」→「系统已完全识别 esp32p4、正在编译」
- **整个移植路线 100% 验证可行**，剩下的是「等网络下载完 + 逐个修编译报错」

## 五、还没做的（下一步）

| 优先级 | 事项 | 说明 |
|--------|------|------|
| 高 | mbedtls 子模块下载 | 后台重试中，GitHub 恢复后自动完成 |
| 高 | 首次完整编译 | 编译报错会逐个修（预期有） |
| 中 | 数据采集（12 类手势） | PC 上就能做，不依赖板子，可并行 |
| 中 | 板级外设适配 | 屏幕/触摸/摄像头/音频（等板子到） |
| 中 | AI 模型训练 | 轻量 CNN + INT8 量化 |
| 低 | 5 大功能开发 | 集成到应用 |

## 六、板子回来前能做什么（按优先级排）

> 核心思路：**不依赖板子的事，现在全做完**，板子一到直接进入真机调试。

| 优先级 | 事项 | 说明 | 需要什么 |
|--------|------|------|----------|
| 高 | 等 mbedtls 下载 + 首次编译通过 | GitHub 恢复后自动完成；编译报错逐个修 | 网络好 + 排错 |
| 高 | **数据采集（12 类手势）** | 用手语图片/自己拍，按类别整理 | 电脑 + 手 |
| 高 | **模型训练** | PC 上训练轻量 CNN，INT8 量化 | 数据集 + 训练环境 |
| 中 | 学 LVGL 界面开发 | 用模拟器练习做界面（识别文字显示、按钮、滑块） | 模拟器（已有） |
| 中 | 学 TFLite Micro 集成 | 读 OpenVela 官方教程，准备模型部署代码 | 官方文档（已有） |
| 中 | 准备预合成语音 | 录/生成 12 段手势对应的 wav | 电脑 |
| 低 | 研究屏幕/摄像头驱动 | 读 PDF 确认 sensor/LCD 型号，找上游驱动 | PDF 资料 |
| 低 | 写演示文档/答辩思路 | 梳理技术方案和亮点 | 电脑 |

**强调**：数据采集 + 模型训练是最值得提前做的——它在 PC 上就能完成，且是项目 AI 部分的核心，不用等板子、不用等编译。

## 七、恢复操作手册（下次接着干，照这个做）

### 1. 检查 mbedtls 是否已下好（后台重试脚本在跑）
```bash
# 看 mbedtls 下载日志（末尾出现 MBEDTLS_READY 就成功了）
tail -5 ~/mbedtls.log

# 确认子模块目录有内容（文件数 > 3）
ls ~/hal_save/components/mbedtls/mbedtls | wc -l
```

### 2. 如果 mbedtls 下好了，继续编译
```bash
# 保存 HAL 到本地 → 清理构建目录（HAL 不会被删）
bash ~/prep_hal.sh          # 若 HAL 已在 hal_save，此脚本会跳过保存

# 用本地 HAL 源编译（不用再连 GitHub 拉主库）
bash ~/build_p4f.sh
```

### 3. 看编译结果
```bash
tail -30 ~/build_p4.log
```

### 4. 编译报错的处理原则
- 报错是**正常的排错过程**，每修一个就离成功近一步
- 把报错截图/复制发给 AI 助手，一起分析

## 八、常用检查命令（在 Ubuntu 终端里）

```bash
# 看源码多大（下完约 34GB）
du -sh ~/openvela

# 看 HAL 克隆进度（下完会有 HAL_READY）
tail -20 ~/hal_clone.log

# 看编译日志
tail -30 ~/build_p4.log

# 看后台还有没有编译进程
ps aux | grep -E "cmake|ninja|git" | grep -v grep | wc -l
```

## 九、第一天的感悟（写给自己的）

1. **移植没有想象中恐怖**——上游有现成代码，大部分是"搬运 + 改配置"
2. **网络是最大变量**——国内访问 GitHub 不稳，要会用"重试 + 浅克隆"
3. **构建系统报错是家常便饭**——今天修了 5 个不同的报错，都是正常的排错过程
4. **一步步来，每解决一个报错就是一次进步**

---

## 十、第二天记录（2026-08-12）

### 今天做了什么

**核心目标**：继续 esp32p4 移植，把昨天"HAL 下载卡住"的问题解决，推进到实际编译。

| 事项 | 说明 | 状态 |
|------|------|------|
| VPN 调试 | 虚拟机的 GitHub 网络一直断；Windows 上 GitHub 用 VPN 后能克隆浅仓库 | ✅ |
| HAL 硬件库通过 Windows 下载 | 在 Windows 上 `git clone --depth 1` + `fetch` 正确版本，压缩成 tar.gz（41MB） | ✅ |
| HAL 传入虚拟机 | guestcontrol copyto 成功，解压到 `nuttx/arch/risc-v/src/chip/esp-hal-3rdparty/` | ✅ |
| esp_start.c 兼容性修复 | 7 个缺失头文件用 `#ifdef` 保护（soc/mmu.h、extmem_reg.h、reg_base.h、spi_flash_mmap.h 等） | ✅ |
| esp_gpio.c GPIO 寄存器差异 | 跳过 P4 的 GPIO 功能选择（P4 用不同的 API） | ✅ |
| esp_irq.c 安全返回值 | 函数末尾加 safety return，消除编译器警告 | ✅ |
| defconfig 参数补全 | 加了 `CONFIG_ESPRESSIF_NUM_CPUS=2`（双核 CPU 配置） | ✅ |
| 首次编译尝试 | 编译启动！通过了配置阶段、HAL 加载，**走到了实际 C 代码编译** | 🟡 |
| mbedtls 子模块下载 | 网络问题：GitHub 连接不稳定，mbedtls（加密库）还没拉到 | ❌ 阻塞 |

### 当前卡点

**唯一阻塞**：HAL 里的 `mbedtls` 子模块（加密库，约 200MB）需要从 GitHub 下载，当前网络不稳定（VPN 没覆盖到虚拟机和 Windows 的 GitHub 流量）。

**不是代码问题**——代码修改全部到位，只是差这个库文件。

### 下一步（等网络好时）

```bash
# 在 Ubuntu 终端里跑（5 分钟搞定）
cd ~/openvela
bash build_final.sh
```

这个脚本已经写好了：先配置 → 放 HAL → 编译。如果 mbedtls 子模块下载也通了，就能出固件了。

### 第二天感悟

1. **网络问题真的能把项目卡死**——一个子模块下不来，后面全停
2. **先在 Windows 上下载，再传进虚拟机** 是绕过网络问题的好办法
3. **每一步编译报错都是真实的移植问题**——头文件、API 差异、配置缺失，都是实际工程经验
4. **板子回来前还有大把事做**——模型训练、数据采集、界面开发都不用等板子

---

## 十一、恢复操作手册（下次继续，照这个做）

### 1. 检查网络
```bash
# 在 Ubuntu 终端测 GitHub 是否通
git ls-remote https://github.com/espressif/esp-hal-3rdparty.git HEAD
```

### 2. 用 VPN 改全局模式再试

### 3. 网络通了直接编译
```bash
cd ~/openvela
bash build_final.sh
```

### 4. 看编译结果
```bash
tail -30 ~/build_p4.log
```

---

## 十二、第三天+第四天记录（2026-08-14 ~ 08-15，与 OpenCode 协作）

### 8-14：硬件资料确认（Windows 侧完成）

| 事项 | 说明 | 状态 |
|------|------|------|
| 解析官方 PDF | `esp-dev-kits-zh_CN-master-esp32p4.pdf`（74页）文字提取 + 页面渲染 | ✅ |
| ⚠️ 重大发现 | 文档是 **P4X 版硬件**（芯片 v3.1+），计划书写的 P4 老版，**板子到手先看丝印** | ✅ |
| 硬件型号确认 | 屏幕 EK73217BCGA+EK79007AD（7寸 1024×600 MIPI-DSI）、音频 ES8311（I2S+I2C）、C6-MINI-1 WiFi 模组、2MP MIPI-CSI 摄像头（sensor 型号待确认）、**板上还有 Ethernet RMII 可兜底** | ✅ |
| 产出 | 新建 `硬件规格速查表_ESP32P4.md`（含待确认清单 5 项） | ✅ |

### 8-15：编译突破 + 进入真代码适配（OpenCode 在 VM 里干）

| 事项 | 说明 | 状态 |
|------|------|------|
| mbedtls 目录结构问题 | 解压多了一层 `espressif-mbedtls-582ff48` 子目录，`psa/crypto.h` 找不到；先删旧目录再 mv 解决 | ✅ |
| 大规模编译通过 | **NuttX 内核 + C++ 库 + NSH 全部编译成功**（证明移植代码本身是对的） | ✅ |
| HAL 启动代码 | 过 `nxtask_init` 关（6 处报错已修） | ✅ |
| HAL 平台适配层 | 卡在 `os.c`：`O_CREAT`/`F_GETFL` 未定义 → 缺 `#include <fcntl.h>` 类头文件 | 🟡 |

### 协作模式定型（重要）

```
OpenCode（Ubuntu 虚拟机内）   → 改源码、跑编译、看报错
我（Windows 侧）              → 分析报错给精确补丁、批量扫雷方案、
                               文档档案、硬件调研、审阅改动
你（人）                      → 决策、真机、网络问题、文件传递
```

> 约定：OpenCode 的产出落盘到 VM 的 `~/vm_exchange/`（STATUS.md / ERROR_LOG.txt / DIFF.txt / CHANGED_FILES.txt / QUESTIONS.md），再传到 Windows 的 `E:\Desktop\Openvela\vm_exchange\`，Windows 侧 AI 直接读。

### 当前卡点 & 下一步

1. **os.c 头文件适配**：批量扫雷（`<fcntl.h>` `<sys/stat.h>` `<unistd.h>` `<errno.h>`），对照 esp32c3/c6/h2 同一 HAL 的过法，或上游 NuttX esp32p4
2. 修完 → 继续编译 → 直到**出固件**（里程碑 M1：板子 NSH 启动）
3. 并行：数据采集 + 模型训练（PC 上做，不依赖板子）

### 感悟（写给自己的）

1. **mbedtls 网络坑彻底绕过去了**——"Windows 下载 → 传进虚拟机"这条路打通后，GitHub 断网不再致命
2. **内核+NSH 全过 = 移植路线 100% 正确**，剩下的是"让 HAL 说 NuttX 的话"，机械但可控
3. **OpenCode 效率提示**：别一个一个报错挤牙膏，要"批量扫雷 + 对照 C6 过法 + diff 上游"，一次修一类

---

## 十三、🎉 固件编译成功（2026-08-15 晚，Windows 侧 AI 直连虚拟机）

### 重大进展：`nuttx.bin` 出炉！

> 通过 VBoxManage guestcontrol **直连 Ubuntu 虚拟机**（openvela / vela 用户），Windows 侧 AI 亲自完成最后一轮排错：
> 从 os.c 卡点一路打通到**固件生成**（`nuttx.bin` 591KB，`MAKE_EXIT=0`）。

### 本轮修复清单（共 10 处，全部已固化进 VM）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `hal.tar.gz`（HAL os.c） | `O_CREAT/F_GETFL` 未定义 + `struct intr_adapter_to_nuttx` 不完整 | 加 `<fcntl.h>` + 定义结构体（**打进 tarball，一劳永逸**） |
| 2 | defconfig | 缺 `ONESHOT`、`ARCH_RV32/ISA`、`NUSER_INTERRUPTS`（configure 不解析 select） | 显式写入全部被 select 的符号（`CONFIG_ARCH_NUSER_INTERRUPTS=256` 等） |
| 3 | `build_p4_final.sh` | PATH 缺 build-tools（kconfig-tweak 找不到）；chip 链接时序脆弱；配置注入"键存在就跳过"bug | 补 PATH + 补 chip 链接 + 注入改为"替换或追加" |
| 4 | `Toolchain.defs` | `-Wstack-usage=` 空参数（CONFIG_STACK_USAGE_WARNING 为空） | 守卫改为 `filter-out 0`（空值和 0 都安全） |
| 5 | esp_oneshot.c | 旧 HAL API：`timer_group_periph_signals` 消失、`timer_ll_*` 改名 | 换新 RCC 宏（`PERIPH_TIMG1_MODULE` + `timg_ll_*`）+ 中断源直用 `ETS_TG1_T0_INTR_SOURCE` |
| 6 | 7 个公共驱动 .c | `soc/*_periph.h` 头文件搬去 `hal/*_periph.h` | 批量 sed 修正 include |
| 7 | esp_irq.c | `g_irqmap` 缺失（MINIMAL_VECTORTABLE 必需）、`esprv_intc_int_*` 全部改名、与 HAL 重复定义、尾部残损 | 加 const identity `g_irqmap`、改名 `esprv_int_*`、删重复函数、清尾部垃圾 |
| 8 | `esp32p4_sections.rev3.ld`(+.tmp) | 缺 `_sinit/_einit`（C++ 初始化段符号） | 补别名（注意构建用的是 **rev3** 版本不是 sections.ld） |
| 9 | esp32p4_appinit.c（新建） | 缺 `board_app_initialize` | 照 esp32s3 模式新建，调 `esp_bringup()`，Make.defs 注册 |
| 10 | VM 环境 | 无 esptool（MKIMAGE 卡住） | pip 升级后装 **esptool 5.3.1** |

### 经验（本轮最值钱的）

1. **configure.sh 不跑 olddefconfig**——Kconfig 的 `select` 不生效，defconfig 必须显式写全所有符号（ARCH_RV32、ISA、NUSER_INTERRUPTS 都是这么漏的）
2. **HAL 8d0a8989 相对 OpenVela 代码的 API 漂移很大**：`soc/*_periph.h`→`hal/*_periph.h`、`esprv_intc_int_*`→`esprv_int_*`、`timer_group_periph_signals`→直接查 soc interrupts.h——都是"批量扫雷 + 编译器提示"解决的
3. **链接脚本要用对版本**：BOARD_REV=.rev3 → 改 `esp32p4_sections.rev3.ld`（含 .tmp 模板）
4. **直接操作虚拟机的效率碾压"拷贝日志-回传"模式**——VBoxManage guestcontrol 一条命令闭环
5. 编译产物备份：Windows `E:\Desktop\Openvela\vm_exchange\nuttx.bin`（591KB）

### 下一步（真机阶段）

1. **烧录**：`esptool.py -c esp32p4 -p COM口 write_flash 0x0 nuttx.bin`（板子到手后）
2. **串口看启动日志**（115200）：验证 `nsh>` 出现 = 里程碑 M1 真机达成
3. 逐外设：屏幕(MIPI-DSI/EK73217BCGA) → 触摸 → 摄像头 → 音频(ES8311) → WiFi(C6)
4. **并行**：数据采集 + 模型训练（纯 PC，现在就开始）

### 附：板子到手前的准备（2026-08-15 下午，逛宜家期间 🛋️）

| 产出 | 位置 | 说明 |
|------|------|------|
| 烧录就绪包 | `烧录就绪包/` | nuttx.bin + `flash_windows.bat` 一键烧录 + README |
| 真机烧录调试手册 | `真机烧录调试手册.md` | 完整流程 + 常见问题 + 启动日志验证点 |
| 数据采集方案 | `数据采集方案.md` | 12 类手势采集规范（每类 ≥150 张） |
| 数据集预处理 | `训练脚本/prepare_dataset.py` | 照片/视频抽帧 + 增强 + 训练/验证切分 |
| 模型训练 | `训练脚本/train_cnn.py` | 轻量 CNN（<300KB）+ INT8 PTQ 量化 + TFLite |
| 语音生成 | `训练脚本/generate_wavs.py` | edge-tts 生成 12 段 WAV（16kHz） |
| 应用骨架 | `应用骨架/sign_translate/` | LVGL v9 + TFLite Micro 手语翻译应用（UI/识别/相机/音频/上传 5 模块 + TODO 清单） |

> 板子到货（2026-08-15 晚）**发现发错货**：收到的不是 ESP 系列板卡（非 ESP32-P4），已准备联系卖家换货。等待正确板卡期间：继续数据采集 + 模型训练 + 应用开发（全部不依赖板子）。
>
> **2026-08-15 深夜 · 代码已提交官方仓库 ✅**：作品已通过 fork+PR 流程合入大赛专属仓库
> `open-vela/contest2026_375_benniaoxianfei` 的 `dev-ai-contest-2026` 分支（PR #2 merged，
> 提交 `550af88e39`）。流程要点：官方仓库 cla-check 规则阻止直推 → 改走官方推荐的
> **fork → 推 fork → PR → 签 CLA → 合入**；合并时遇 rebase 报错 → 历史线性化后成功。

---

## 十四、8-16 记录：提交合规 + 训练实验结论

### 当日完成

| 事项 | 说明 |
|------|------|
| README 作品说明 | 替换官方模板，含 5 功能/技术亮点/运行方式（官方要求） |
| AI Coding 日志 | 导出 DSH 会话（24985 行）到 `logs/931258065/`，**脱敏令牌(103处)+密码(365处)** |
| 推理验证脚本 | `训练脚本/infer_test.py`——单图 INT8 推理（与板端同流程） |
| 12 段语音 | edge-tts 生成 `训练脚本/语音/*.wav`（16kHz 16-bit，33-41KB/段） |
| 网页日志服务 | `网页服务/` Flask+SQLite，POST 接收 + 网页 3 秒刷新（已实测） |
| 答辩材料 | `答辩材料/答辩提纲.md`：演示脚本/亮点/问答/素材清单 |
| 拍摄指南 | `拍摄指南_给帮忙的人.md`（明天多人拍摄用） |
| 管线升级 | ① 中文路径读写修复（cv2 通病）② 数字手势禁翻转 ③ 跳视频片头帧 ④ 视频级切分（≥3 单元）/帧级兜底 |

### 训练实验（三次，结论确定）

| 实验 | 数据 | 验证集准确率 | 实际表现 |
|------|------|-------------|---------|
| 冒烟 | 2 类×1 视频 | 100% | 数字1✅ 谢谢❌ |
| 9 类 | 1-9×1 视频 | 95.1% | **全部判成"7"** |
| 10 类 | 1-9+谢谢×1 视频 | 93.4% | **全部判成"1"**（谢谢也 96% 判成 1）|

**确定性结论（8-16 晚修正）**：
1. **管道 100% 验证**——视频→抽帧→增强→训练→INT8(304KB)→推理全链路正常
2. **早期"塌缩"根因 = 量化输入范围 bug（代码问题，非纯数据问题）**：INT8 校准集误用归一化 [0,1]，
   而推理喂 [0,255] 像素 → 输入错位 → 模型塌缩到单类。修复（校准用原始像素）后**模型可正常区分手势**
3. 双人数据（每类 2 视频，视频级切分）后：**浮点模型跨人验证 95.5%**（真实泛化）；
   任意单帧实测 浮点 70% / INT8 50%（帧质量 + 量化损失 + 相似对 2/3、4/5、7/9、6/8 混淆）
4. **改进方向**：INT8 损失可用 QAT 或板端浮点部署（P4 内存够，模型 1.2MB）；相似手势对靠更多数据

### 待办

- [x] ~~明天：多人拍摄（每类 2-3 视频，多背景）~~ —— **已完成（双人数据：一飞 + 芸芸）**
- [ ] 补 0_零、你好 素材（当前只有 1-9 + 谢谢）
- [ ] 周一：正确板子到货 → 真机烧录验证（烧录就绪包已备）
- [ ] 待网络好：本地新增 12+ 个 commit 走 fork→PR 合入官方仓库

---

## 十五、8-17 记录：板子已邮寄 + 训练定稿

### 事件

- **✅ 正确开发板已邮寄**：预计 **8-18（周一）到货**。到货后立即：烧录 `nuttx.bin`（烧录就绪包已备）→ 串口 115200 看 `nsh>`（真机 M1）→ 外设逐个点亮
- **✅ 双人素材到位**：一飞（数字1-9+谢谢）+ 芸芸（数字1-9+手势谢谢），10 类 × 2 人 = 20 视频

### 训练定稿（三个口径）

| 口径 | 准确率 | 说明 |
|------|--------|------|
| 浮点模型 · 跨人验证集 | **95.5%** | 标准报告口径（验证集=另一人视频，无泄漏） |
| 浮点模型 · 任意单帧 | 70% | 苛刻测试（可能抽到模糊/过渡帧） |
| INT8 模型 · 任意单帧 | 50% | 板端部署版，量化损失 ~20% |

### 周末/等板期间可做

1. **补数据**：相似对（2/3、4/5、7/9、8）多拍；缺 0_零、你好
2. **QAT 量化感知训练**：把 INT8 拉到接近浮点（~70% 方向）
3. **板端应用集成准备**：defconfig 加 LVGL/TFLM/应用配置，模拟器验证 UI
4. **日志提交**：网络好时推 PR（本地已 12+ commit）

---

## 十六、8-16 记录：INT8 量化缺口修复（50%→94.4%）+ 模拟器集成启动

> 分工：② QAT + ③ 模拟器集成 = 我来；① 补数据 = 用户下午手机传

### ② 量化一致性修复（关键）

**根因定位**（板端 recognition.cxx + infer_test.py + 转换脚本三处对照）：
1. 模型训练输入是归一化 [0,1]，但板端/推理喂的是原始像素 [0,255] —— 输入域错位，INT8 掉到 50%
2. 板端代码硬编码 INT8 输入（钳位 [-128,127]），模型实际是 UINT8 输入 —— 像素 128~255 被截断
3. 板端量化公式写错：`q=(pix-zp)/scale`，应为 `q=pix/scale+zp`

**修复**：
- 模型第一层加 `Rescaling(1/255)`：模型输入统一为原始像素 [0,255]，训练/校准/推理全链路一致
- `recognition.cxx` 重写：输入/输出张量按模型实际类型（UINT8/INT8）自适应 + 正确量化公式
- 重训后三档对比（跨人验证集）：

| 口径 | 8-15 旧 | 8-16 新 |
|------|--------|--------|
| 浮点模型 | 95.5% | 94.7% |
| PTQ INT8 | 50% | **94.4%**（输入 uint8 scale=1.0 zp=0 ✓） |
| QAT INT8 | 未做 | 进行中（tfmot 与 Keras3 不兼容→强制 Keras2 重训） |

**QAT 踩坑记录**：tfmot 0.8.1 只支持 Keras2(tf_keras) → 脚本设 `TF_USE_LEGACY_KERAS=1`；数据加载用 Keras3（其 loader 兼容中文路径）；Rescaling 层需 NoOpQuantizeConfig + quantize_scope 注册。

### ③ 模拟器集成（OpenVela goldfish-arm64-v8a-ap）

**重要发现**：OpenVela 树自带 TFLM 官方支持！
- `apps/mlearning/tflite-micro/`（CONFIG_TFLITEMICRO，依赖 SYSTEM_FLATBUFFERS + MATH_GEMMLOWP + MATH_KISSFFT + MATH_RUY）
- 官方文档 `docs/zh-cn/edge_ai_dev/`（含集成示例：DEPENDS tflite_micro + DEFINITIONS）
- goldfish 模拟器 defconfig 已开 LVGL（CONFIG_GRAPHICS_LVGL + LV_USE_NUTTX + 触摸）

**已完成**：
- 应用按 OpenVela 规范补 Kconfig + CMakeLists（DEPENDS lvgl tflite_micro）
- `lvgl_init` 从桩改为 lvgldemo 同款 `lv_nuttx_init`（显示/触摸自动挂载）
- 应用源码部署到 VM `apps/examples/sign_translate/`
- goldfish defconfig 追加：EXAMPLES_SIGN_TRANSLATE / TFLITEMICRO / FLATBUFFERS / GEMMLOWP / KISSFFT / RUY
- 模拟器全量重建进行中（TFLM 源文件多，预计 20+ 分钟）

### 待办
- [ ] QAT 结果对比（若 >94.4% 则替换模型）
- [ ] 真实模型 data → model_data.cc → 重编译 → 模拟器跑 UI 验证
- [ ] 下午：0_零、你好 + 相似对数据 → 重训（12 类）
