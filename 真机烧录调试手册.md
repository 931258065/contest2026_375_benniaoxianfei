# 真机烧录调试手册（板子到手版）

> 固件：`E:\Desktop\Openvela\vm_exchange\nuttx.bin`（591KB）　生成：2026-08-15
> 目标：烧录 → 串口看到启动日志 → `nsh>` 出现 = **真机里程碑 M1**

---

## 第一步：硬件核对（5 分钟，烧录前必做）

| 核对项 | 怎么看 | 记录到 |
|--------|--------|--------|
| 板卡丝印是 **P4** 还是 **P4X**？ | 板子正面丝印 | 速查表待确认① |
| 摄像头 sensor 型号 | 摄像头模组背面丝印（GC/OV/SC 开头） | 速查表待确认② |
| 屏幕触摸 IC | 屏幕排线附近丝印 | 速查表待确认⑤ |
| 组装：屏幕/摄像头排线 | 按 PDF 组装（屏幕反向线序、摄像头同向线序） | —— |

## 第二步：连接电脑

1. USB-C 线连板子（烧录用 USB 串口/JTAG 口）
2. Windows 设备管理器 → 端口(COM和LPT) → 看新出现的 COM 口（板载 USB-Serial/JTAG，一般免驱；无则装 Espressif USB 驱动）
3. 记下 COM 口号（如 COM3）

## 第三步：烧录（二选一）

### 方案 A：Windows 直烧（推荐，简单）
```powershell
# 1. 装 esptool（联网一次）
pip install esptool

# 2. 烧录（COM 口换成你的）
esptool.py -c esp32p4 -p COM3 -b 460800 write_flash 0x0 E:\Desktop\Openvela\vm_exchange\nuttx.bin
```
成功标志：`Hash of data verified.` / `Leaving... Hard resetting`

### 方案 B：虚拟机烧录（我可以代劳）
- 用 VBoxManage 把 USB 串口设备挂给 openvela 虚拟机
- VM 里 `/dev/ttyUSB0` 出现后：`esptool.py -c esp32p4 -p /dev/ttyUSB0 write_flash 0x0 /home/vela/nuttx.bin`
- 需要先 `VBoxManage list usbhost` 找到 USB 设备 ID，再 `usbattach`

## 第四步：看启动日志（关键！）

### Windows：PuTTY / MobaXterm / VS Code 串口终端
- 端口：COM3　波特率：**115200**　8N1

### 虚拟机：minicom
```bash
sudo minicom -D /dev/ttyUSB0 -b 115200
```

### 日志里找什么
```
✅ 出现 NuttX 版本横幅 + "nsh>" 提示符   → 真机 M1 达成！
⚠️ 卡在某处（如 uart/clock/spiflash）    → 完整日志复制给我，我分析
❌ 无输出                              → 检查供电/烧录地址/波特率
```

## 烧录后马上要测的命令（nsh> 里）

```bash
help              # 命令列表
ls /dev           # 设备节点
ifconfig          # 网络（现在可能没有）
gpio               # GPIO 测试（如有）
```

## 常见问题

| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 烧录报 "Failed to connect" | 没进下载模式/线不行 | 按住 BOOT + 按 RESET 再试 |
| 烧录成功但串口无输出 | 波特率错/串口选错 | 确认 115200 + 正确 COM 口 |
| 启动卡死在 ROM 阶段 | 固件/时钟问题 | 完整日志给我 |

## 并行准备（不等烧录，现在就能做）

1. **数据采集**：12 类手势拍照/收集，每类 ≥100 张
2. **模型训练**：轻量 CNN + INT8（PC 上）
3. **12 段 WAV 语音**：TTS API 生成
4. **LVGL 应用骨架**：界面代码先写
5. **网页日志服务**：Flask 先搭
